<html>
    <head>
        <title>WEATHER</title>
        <link rel="icon" href="./img/weather.png">
        {{!-- <link rel="stylesheet" href="../public/css/styles.css">  --}}
        {{!-- Here we are trying to grab css asset from ./public/css/styles.css to index view =>it will not work --}}
        
        <link rel="stylesheet" href="./css/styles.css"> 
         {{!-- now can grab static asset css from ./public/css/styles.css to src/app.js   --}}
    </head>
    <body>
        {{!-- <div class="main-content"> --}}
            {{!-- //we can include our partial in views with synatx {{>partial}} or {{>header}} --}}
        {{!-- {{>header}} --}}
        {{!-- <p>Use this site to get ypur weather...</p> --}}
        {{!-- <p><h3>{{title}}</h3> by {{name}}</p> --  //this included in header partial ,so we dont have to call seprately--}}
        {{!-- </div> --}}
        
        {{!-- we want our footer at bottom --}}
        {{!-- we can do that by puting all body in div except footer, then we use flex box 
        which will take all space except what footer needs--}}
        {{!-- {{>footer}} --}}

        <div class="main-content">
            
        {{>header}}
        <p>Use this site to get your weather...</p>
        <form>
            <input placeholder="Location...">
            <button>Search</button>
        </form>
        <p id='message-1'></p>
        <p id='message-2'></p>
        </div>
        
        {{>footer}}
        <script src="./javascript/scipts.js"></script>   
        <!--//linking css,js with relative route,absolute route may expose hard disk etc-->
    </body>
</html> 